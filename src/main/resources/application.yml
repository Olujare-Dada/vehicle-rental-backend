# Active Profile
spring:
  profiles:
    active: default

  # Jackson Settings
  jackson:
    time-zone: America/Toronto
    date-format: "yyyy-MM-dd"
    deserialization:
      fail-on-unknown-properties: false      
      
  # Email Settings
  mail:
    username: kalelofearth95@gmail.com
    password: mdcr gnqd paxu bypj
    host: smtp.gmail.com
    port: 587
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          debug: true
          transport:
            protocol: smtp

  # Common Database Settings
  sql:
    init:
      continue-on-error: false

  jpa:
    show-sql: true
    open-in-view: false
    properties:
      jakarta:
        persistence:
          sharedCache:
            mode: ENABLE_SELECTIVE
      hibernate:
        format_sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

  # File upload configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

---
# Default Profile Settings
spring:
  config:
    activate:
      on-profile: default

  datasource:
    url: jdbc:postgresql://localhost:5432/vehicleManagementDb
    username: postgres
    password: admin
    hikari:
      maximum-pool-size: 2

  jpa:
    hibernate:
      ddl-auto: validate

# Python Flask service configuration
python:
  flask:
    base-url: http://localhost:5000